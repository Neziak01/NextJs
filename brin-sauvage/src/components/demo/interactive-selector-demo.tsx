import InteractiveSelector from "@/components/ui/interactive-selector";

/** Le sélecteur seul, avec ses données par défaut. */
const DemoOne = () => {
  return (
    <div className="h-min-screen w-full">
      <InteractiveSelector />
    </div>
  );
};

export { DemoOne };
